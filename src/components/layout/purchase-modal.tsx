/**
 * DEPRECATED — Purchase modal has been moved to use-require-plan.tsx.
 *
 * The PurchaseModalOverlay component lives inline in PlanGateProvider
 * to avoid circular dependencies. This file is kept to prevent import
 * errors in any stale references.
 */
export function PurchaseModal() {
  return null;
}
